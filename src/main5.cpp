#include <iostream>
#include <cmath>
#include <algorithm>
#include <cstdlib>
#include <ctime>
#include "task5.h"
#include <string>

using namespace std;


int main()
{
  /*  char* buf = "";
    int N = 0;
    char** result = nullptr;
    split(&result, &N, buf, ',');

    for (int i = 0; i < N; i++)
        printf("%s\n", result[i]);*/
	return 0;
}