#include <iostream>
#include "task5.h"
using namespace std;

int main() {

	/*char** substr_array = new char* [20];
	for (size_t i = 0; i < 20; i++)
	{
		substr_array[i] = new char[30]{ 0 };
	}
	char* str = new char[22]{ "Pasha went for a walk" };
	int count(0);
	split(&substr_array,&count,str, ' ');
	cout << count << " Strings:" << endl;
	for (size_t i = 0; i < count; i++)
	{
		cout << substr_array[i] << endl;
	}*/
}