#include <iostream>
#include "task2.h"

int main()
{

	std::cout << (checkPrime(2) ? "true" : "false") << "\n";
	std::cout << (checkPrime(3) ? "true" : "false") << "\n";
	std::cout << (checkPrime(12) ? "true" : "false") << "\n";



	return 0;
}