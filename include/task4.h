#include <iostream>
#include <cstdlib>
#include <string>
#include <cstring>
using namespace std;
char* sum(char* x, char* y);
char* sum_pro(char* min, char* max, int dif, int len);
int dig(char x);
char to_char(int x);
char* del(char* arr, int len);